# 旺旺家庭聊天小助手

一个 Vite + React 的本地规则版聊天页面，用来给家人解释旺旺近况。当前版本不接 API，构建后可直接静态部署。

## 本地运行

先安装依赖：

```bash
npm install
```

启动开发服务器：

```bash
npm run dev
```

然后按终端提示打开本地地址。

## 构建

```bash
npm run build
```

构建产物会生成在：

```bash
dist
```

## 静态部署

部署时使用 `dist` 目录作为静态站点目录即可。
